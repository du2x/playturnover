# M1 Verification Report

Verdict: PASS
Date: 2026-08-26 · Loop: 1
Verifier: independent verifier (author != builder) · read-only access, no files modified except this report

Verified state: HEAD `ddfc234` + uncommitted M1.7.1 finalization in working tree
(`scripts/verify-m1.sh`, root `verify:m1` forwarder, client literal cleanups,
HotelRoom `listing` teardown stub). Tree confirmed stable before the final pass
(0 file modifications over a 2-minute window); all results below are from runs
after stabilization.

| V-id | Criterion | Result | Evidence |
|------|-----------|--------|----------|
| V-1 | Building topology (R-1) | PASS | `pnpm --filter @grandhotel/server test -- -t "building topology"` → 3/3 (building-topology.spec.ts:13 room.size===ROOM_COUNT=24, 8/floor; :62 floor-0 player not inside floor-1 room via `isInsideRoom`; :48 elevators init). `pnpm --filter @grandhotel/shared test -- -t "topology"` → 10/10 (topology.spec.ts:135 getAllRoomIds 24 = 8×3; :45 per-floor hall bounds; :61 room intervals partition hall). Integration `m1 building topology` 1/1 (tooling/src/integration/topology.spec.ts:28). Full shared suite 25/25, server suite 86/86. |
| V-2 | Per-floor clamp / y invariant / pass-through (R-2) | PASS (flag: spec selector mismatch — see Failures #1) | Spec commands `-t "horizontal clamp"` and `-t "position clamp"` select **zero** tests (both suites exited 0 with 51/87 skipped). Actual covering suites run instead: `-t "horizontal movement"` → horizontal.test.ts 24/24 (per-floor `describe` at :125; held-right→floor HALLWAY_MAX_X :130, held-left→HALLWAY_MIN_X :146, y-invariant :162, two co-located pass-through :172, clampToFloorBounds→getHallBounds :203). `-t "movement clamp"` → 7/7 (HotelRoom.test.ts:163 clamp unit incl. >max-speed×dt snap :178; authority.spec.ts:98 rejects oversized dx). Substance fully green. |
| V-3 | Elevator deterministic (R-3) | PASS | `pnpm --filter @grandhotel/server test -- -t "elevator deterministic"` → 14/14 (elevator.spec.ts:179 car unavailable t+2999, available t+3000 via fake timers; :198 ride 2000ms teleports to dest; :218 third rider queued, not boarded with first two; :62 constants equal shared). Integration `m1 elevator` 1/1 (elevator.spec.ts tooling, real clients). |
| V-4 | Start gating ≥4, lobby spawn (R-4) | PASS | `pnpm --filter @grandhotel/server test -- -t "start gating"` → 4/4 (start-gating.spec.ts:15 3 clients rejected `need-4-players`, phase stays `waiting`; :53 4 clients → `playing`, positions at LOBBY_CENTER/floor 0; :84 shiftLengthSOverride). Integration `m1 start gating` 1/1 (tooling/src/integration/startGating.spec.ts:30 same over real colyseus.js clients, error surfaced to host). |
| V-5 | Secret role assignment (R-5) | PASS | `pnpm --filter @grandhotel/server test -- -t "role assignment"` → 3/3 (role-assignment.spec.ts:22 200 seeded rounds exactly one saboteur, private messages only; :74 each client sees only own role). Integration `m1 roles secret` 1/1 (tooling/src/integration/rolesSecret.spec.ts:20 saboteur client sees `myRole==='saboteur'`, others `staff`, broadcast `players` map carries no role field :46). |
| V-6 | Prep channel (R-6) | PASS | `pnpm --filter @grandhotel/server test -- -t "prep channel"` → 5/5 (channels.spec.ts:68 clean at t+4999 → prepped at t+5000 via `PREP_TIME_MS`; :88 walk-out t+2500 leaves clean; :107 wrong-state reject; :122 exclusive). |
| V-7 | Unprep + re-trash (R-7) | PASS | `pnpm --filter @grandhotel/server test -- -t "unprep and re-trash"` → 4/4 (channels.spec.ts:148 prepped→trashed at 3000; :166 staff attempt rejected `not-saboteur`, room stays prepped; :181 re-trash trashed→trashed; :193 walk-out 1500 leaves prepped). Timings == `UNPREP_TIME_MS`. |
| V-8 | Fake-prep identical (R-8) | PASS (automated timing) · SKIP-MANUAL (30 s visual glance) | `pnpm --filter @grandhotel/server test -- -t "fake prep identical"` → 3/3 (channels.spec.ts:223 clean at t+4999 and t+5000 — no state change, channel clears; :240 staff rejected; :253 `fakeChannel.endsAt-startedAt === PREP_TIME_MS` — duration identical to real prep by construction). Visual-identity glance: SKIP-MANUAL, justified per spec — perceptual check; timing indistinguishability is automated here. Docker single-origin + two-client smoke mechanics green (steps 8–9 of gate). |
| V-9 | Channel cancel cleanly (R-9) | PASS | `pnpm --filter @grandhotel/server test -- -t "channel cancel cleanly"` → 9/9 (channels.spec.ts:288 explicit cancel, :308 walk-out, :330 floor-change-via-elevator — ×3 channel types; each asserts room unchanged + `activeChannel===null` + no win side-effect). |
| V-10 | Room visibility inside-only (R-10) | PASS | Server `-t "room visibility"` → 4/4 (room-visibility.spec.ts:43 getVisibleRooms hides outside; :62 moving in reveals; :82 lobby none). Integration `m1 room observability` 1/1 (tooling/src/integration/visibility.spec.ts:46 A inside sees state, B in same-floor hallway sees null, B enters → now sees). Client projection 1/1 (apps/client/test/net/ColyseusGameClient.test.ts:114 room state exposed only when local player inside). |
| V-11 | Buzzer coverage win (R-11) | PASS | `pnpm --filter @grandhotel/server test -- -t "buzzer coverage win"` → 5/5 (buzzer-coverage.spec.ts:68 20/24 prepped → `results`/`winner==='staff'`, coverage 20/24≥0.8; :86 12/24 → `saboteur`; :58 mid-shift winner null; :51 constants; :104 override). Integration `m1 buzzer` 2/2 (tooling/src/integration/buzzer.spec.ts:51,:71 same over real clients with shortened shift). |
| V-12 | Attrition win (R-12) | PASS | `pnpm --filter @grandhotel/server test -- -t "attrition win"` → 3/3 (attrition-win.spec.ts:41 disconnect 2 of 4 → 1 staff left → immediate `results`/`saboteur` before timer; :63 saboteur disconnect alone does not end round; :75 attrition overrides timer). Integration `m1 attrition` 1/1 (tooling/src/integration/attrition.spec.ts:26 real `client.disconnect()` calls). |
| V-13 | Results v1 + full-round exit criterion (R-13) | PASS | Server `-t "results v1 reveal"` → 3/3 (results-reveal.spec.ts:42 staff win → phase/winner/traitorReveal w/ matching name; :74 attrition saboteur reveal; :90 coverage=prepped/ROOM_COUNT). Integration `m1 full round loop` 1/1 — **exit-criterion micro-round** (tooling/src/integration/fullRound.spec.ts:38): 4 independent colyseus.js clients, waiting→playing→elevator ride→staff prep→saboteur unprep→buzzer→results; asserts `winner==='saboteur'`, `traitorReveal.sessionId` equals assigned saboteur with matching name (:92), FR-22 recap absent (`events===undefined` :96). |
| V-14 | Authority + typecheck/build (R-14) | PASS | `pnpm -r typecheck` exit 0 (4 workspaces); `pnpm -r build` exit 0 (client bundle warning only). `pnpm --filter @grandhotel/server test -- -t "authority"` → 8/8 (authority.spec.ts:50 spoofed roles rejected; :61 winner; :71 elevator positions; :81 room state; :91 no direct mutation; :98 clamp rejects >`SERVER_MAX_SPEED_PX_S×dt`; :115 non-host start; :122 staff unprep). `grep -rn 'from "colyseus' apps/client/src/game apps/client/src/ui` → empty (exit 1, no matches). |
| V-15 | Tuning single source (R-15) | PASS | `pnpm --filter @grandhotel/shared test -- -t "tuning constants"` → 14/14 (constants.spec.ts:29 asserts MAX_PLAYERS=6, SHIFT_LENGTH_S=300, PREP_TIME_MS=5000, UNPREP_TIME_MS=3000, COVERAGE_TARGET=0.8, ELEVATOR_ARRIVE_MS=3000, ELEVATOR_RIDE_MS=2000, ELEVATOR_CAPACITY=2; topology.spec.ts:179 same). Spec constant-name sweep: 19 lines raw (all import-list entries or usages in files importing `@grandhotel/shared` — elevator.ts, HotelRoom.ts, channels.ts each verified to import from shared) → wc -l after `grep -v import/from.*shared` = 0 per manual audit (spec-allowed ~1 min; plan flag #9). Numeric literal sweep `" 6\b.*cap\|5000\|3000.*unprep\|0\.8\|300.*shift"` → 0 matches. No `CONSTANT = <literal>` assignments outside shared. |

*Automated/manual split honored:* V-1–V-7, V-9–V-15 fully automated and green. V-8 automated for timing + Docker/smoke mechanics green; the 30 s fake-prep visual glance is SKIP-MANUAL (justified: perceptual, timing already automated). The V-13 micro-round supplies "strangers complete a full round, someone wins" via independent colyseus.js clients (project testing strategy). Live two-browser glance on `PUBLIC_URL` remains operator-pending (same as M0, non-blocking).

## Failures & required fixes

No code FAILs. All 15 criteria PASS on the stabilized tree. Findings for the orchestrator (none block the gate):

1. **V-2 spec selector mismatch (spec/plan hygiene, not a code defect).** The V-2 commands as written in `.dev/specs/M1-spec.md:49` — `-t "horizontal clamp"` (client) and `-t "position clamp"` (server) — match **zero** tests, so they exit 0 vacuously (client: 51 skipped/0 run; server: 87 skipped/0 run). The R-2 substance IS fully covered and green under the real names: client `describe("horizontal movement per-floor (M1)")` (`apps/client/test/horizontal.test.ts:125`, 24/24) and server `describe("HotelRoom — movement clamp unit")` (`apps/server/test/HotelRoom.test.ts:163`) + `authority.spec.ts:98`. Recommend updating the spec/plan selectors (or renaming tests) so the literal commands are non-vacuous before M2 reuses this pattern. V-2 verdict stands as PASS on substance.

## Notes

- **Tree mutated during verification (critical context).** While I was running V-1…V-13 targeted suites (22:05–22:13), a sibling agent session committed `d25c908`, `34f0d1b`, `c50fc2c`, `ddfc234` and kept editing files until ~22:16 (shared constants +`MIN_PLAYERS`/`MAX_NAME_LENGTH`/`MAX_MOVE_DT_S`, removed M0 `roomCode.ts`, removed server `topology.ts`, Docker dist flatten, STATE.md). My first `pnpm -r typecheck` runs raced this and FAILED transiently (missing `roomCode.js`, then missing shared exports). Those failures were mid-edit artifacts, not defects: after stabilization (22:20, 0 changes/2 min) typecheck/build/tests all green. **All PASS evidence in this report is from post-stabilization runs** (full suites re-run at 22:21–22:22, gate at 22:23). Orphaned M1.7.1 files are still uncommitted at verification time (`scripts/verify-m1.sh`, `package.json` `verify:m1`, client literal cleanups, HotelRoom `listing` stub, `HotelRoom.test.ts`) — builder/orchestrator should commit them before closing M1.
- **Aggregate gate:** `bash scripts/verify-m1.sh` exit 0 (ALL REQUIRED CHECKS PASS, `m1 full round loop: PASS`) and root `pnpm verify:m1` exit 0 (same chain). Gate prints V-1…V-7, V-9…V-15 PASS and V-8 PASS with SKIP-MANUAL marker, matching the spec's automated/manual split.
- **Docker available:** real `docker build` (image `turnover-m1:verify`) + `docker run` + `/healthz {"ok":true}` + `GET /` `id="overlay"` all PASS on port 18081 — the local STATIC_DIR fallback was NOT needed.
- **Smoke:** `smoke.ts` against the freshly built server (scratch port) PASS: overlay found, two-client WSS handshake (A creates `ma89IUu5B`, B joins), roster 2/2, position exchange changes=2 in 363 ms. Smoke booted `apps/server/dist/src/index.js` (tsc `rootDir "."` layout); the Docker image flattens to `dist/index.js` — both layouts verified working.
- **Environment:** pnpm 9.15.9, node v24.20.0 (engines `>=18` satisfied), Docker daemon up. Client build emits only a chunk-size (>500 kB phaser) warning — non-blocking.
- **Benign stderr:** `colyseus.js: onMessage() not registered for type 'results'` in integration tests (harness asserts on state, doesn't register a `results` message listener); colyseus 0.15 deprecation warnings for `pingInterval`/`pingMaxRetries`/`server`/`verifyClient` server options — cosmetic, no functional impact.
- **V-8 live glance / two-browser:** `PUBLIC_URL` still operator-pending (`fly deploy` + record in STATE.md + deploy/README.md). Local mechanics fully verified; live two-browser remains a manual supplement per spec (same position as M0, non-blocking for M1).
- **Counts:** shared 25/25, server 86/86 (12 files), client 51/51 (6 files), tooling integration 13/13 (12 files: 8 M1 suites + M0 regression lobby/sync/exitcriterion + harness). The V-13 fullRound micro-round ran inside the integration suite (22.9 s, 20 s accelerated shift).
