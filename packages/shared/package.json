{
  "name": "@grandhotel/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json --outDir dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@colyseus/schema": "^0.5.70",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^1.6.0"
  }
}
